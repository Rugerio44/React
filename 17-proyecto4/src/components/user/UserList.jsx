import React from 'react';
import avatar from "../../assets/img/user.png";
import { Global } from "../../helpers/Global";

export const UserList = ({ users, loading, hasMoreUsers, auth, follow, unfollow, nextPage }) => {
    
  return (
    <>
     
    </>
  );
};